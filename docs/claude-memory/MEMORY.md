- [ms-auth design rationale](ms_auth_design.md) — opaque refresh tokens, atomic GETDEL rotation, gateway JTI parsing without JWT lib, and why ms-auth gates on keycloak feature flag
- [cross-cutting config pattern](cross_cutting_config_pattern.md) — why CrossCuttingConfigProcessor exists (path vs content gap), @Order(60) timing, line-based YAML block remover, docker-compose `volumes:` overload gotcha, where to extend, the per-resource regeneration of realm/test-all.sh/AggregateController, the dual-source trap (editing the static test-all.sh template is a no-op when resources[] is set), and the zombie-:8080-serves-stale-jar verification gotcha
- [resource-expand db variants](resource_expand_db_variants.md) — ResourceExpandProcessor clones service-a/ per resource; Mongo (document pkg + String id) and UUID (imports) and Mongo application.yml (single-brace placeholders) must be propagated to every file referencing the entity, or it won't compile/start
- [keycloak realm reimport](keycloak_realm_reimport.md) — generated Keycloak imports the realm only into an empty db; a stale keycloak_db_data volume hides new per-service users (admin/batch log in, test-<service> fails) — fix with clean-docker.sh, not code
- [template decompressed + dotfiles](template_decompressed_dotfiles.md) — template is a decompressed dir loaded by TemplateLoader; ship dotfiles as `dot-gitignore`/`dot-env` (decoded on load) to survive plexus default excludes at the resources AND jar plugin layers
- [Keycloak hostname must match iss](keycloak_hostname_iss_match.md) — `KC_HOSTNAME: http://localhost:8089` is load-bearing; without the port, ms-auth-issued tokens get `iss=http://localhost:8080` and every resource server returns 401
- [sync memory to repo](sync_memory_to_repo.md) — feedback: after any memory file write, mirror to docs/claude-memory/ + commit/push without asking (durably authorized)
